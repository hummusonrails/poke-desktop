import XCTest
@testable import PokeDesktop

final class PokeDesktopTests: XCTestCase {
    func testAppLaunches() throws {
        // placeholder test validates that the module imports correctly
        XCTAssertTrue(true)
    }
}
